# _config_aws.yml  — for AWS / Amplify
url: "https://vaxogene.com"   # your AWS or CloudFront domain
baseurl: ""                  # IMPORTANT — root hosting

# Optional: bump assets version to force refresh on prod
assets_version: "26"
